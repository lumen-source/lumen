fn main(console: Console) -> Unit {
  let a = array(3)
  aset(a, 0, 1.5)
  aset(a, 1, 2.5)
  aset(a, 2, 3.0)
  var sum = 0.0
  var i = 0
  while i < alen(a) {
    sum = sum + aget(a, i)
    i = i + 1
  }
  console.print_int(round(sum * 100.0))
}
