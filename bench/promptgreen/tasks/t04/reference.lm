fn in_range(x: Int, lo: Int, hi: Int) -> Int {
  if x >= lo and x <= hi {
    return 1
  }
  return 0
}

fn main(console: Console) -> Unit {
  console.print_int(in_range(5, 1, 10))
  console.print_int(in_range(15, 1, 10))
}
