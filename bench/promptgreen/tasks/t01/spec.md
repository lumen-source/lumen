# t01: int arithmetic

Write a Lumen program with:

- a function `combine(a: Int, b: Int, c: Int) -> Int` that returns `(a + b) * c`
- a `main(console: Console) -> Unit` that prints `combine(3, 4, 5)` with `console.print_int`

Print nothing else.
