# t03: text

Write a Lumen program with:

- a function `greet(name: Text) -> Text` that returns `"hello, " + name + "\n"` built with
  `text_concat`
- a `main(console: Console) -> Unit` that prints `greet("lumen")` with `console.print`

Print nothing else.
