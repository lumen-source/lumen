# t02: while loops

Write a Lumen program with:

- a function `sum_upto(n: Int) -> Int` that returns the sum `1 + 2 + ... + n` using a
  `while` loop (not recursion)
- a `main(console: Console) -> Unit` that prints `sum_upto(20)` with `console.print_int`

Print nothing else.
